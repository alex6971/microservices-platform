server:
  port: 7200

spring:
  application:
    name: log-center
  elasticsearch:
    rest:
      uris: ${zlt.elasticsearch.uris}
      username: ${zlt.elasticsearch.username}
      password: ${zlt.elasticsearch.password}    
